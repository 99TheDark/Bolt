# Comments
```
// Comments are like in JavaScript and TypeScript
// Single-line comments start with double forward slashes, and end on the next line

/*
 * However multiline-comments start with a forward slash and asterisk,
 * and end with an asterisk and forward slash.
 */
```