/*
Variable declaration
*/
let x = 5 * (3 - 12) / 6
let y = true | false & true
let z = x

z += 5

// Always true
if(x < z) {
    y = !y
}

string intro = "Hello world!"
string multiline = "
    Strings are automatically multiline since most 
    languages error when you put a new line midway through anyways
"

let evensum = 0
number i = 0
while(i < 10) {
    if(i % 2 == 0) {
        evensum += i
    }
    i += 1
}