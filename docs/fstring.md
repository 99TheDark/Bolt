# Formatted Strings
```
// Identical to JavaScript/TypeScript formatted strings
string firstName = "Bob"
string lastName = "Jackson"

// Without formatted strings
print("Their name is " + firstName + " " + lastName + "!")

// With formatted strings
print("Their name is ${firstName} ${lastName} !")
```